import { StoriesFilterPipe } from './stories-filter.pipe';

describe('StoriesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StoriesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
